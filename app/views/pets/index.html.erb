<h1>All pets</h1>
<% @pets.each do |pet| %>
  <h2><%= pet.name %></h2>
  <p>Breed: <%= pet.breed %></p>
  <p>Image: <%= pet.image %></p>
  <%= link_to "More Info", pet %>
  <%= link_to "New Pet", new_pet_path %>
<% end %>